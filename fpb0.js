function verses(number)
{
    try
    {
        temp = verses1(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses2(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses3(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses4(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses5(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses6(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses7(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses8(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses9(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses10(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses11(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses12(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses13(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses14(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses15(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses16(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses17(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses18(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses19(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses20(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses21(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses22(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses23(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses24(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses25(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses26(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses27(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses28(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses29(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses30(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses31(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses32(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses33(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses34(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses35(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses36(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses37(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses38(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses39(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses40(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses41(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses42(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses43(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses44(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses45(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses46(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses47(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses48(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses49(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses50(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses51(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses52(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses53(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses54(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses55(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses56(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses57(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses58(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses59(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses60(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses61(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses62(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses63(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses64(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses65(number);
        if(temp != '')
        {
            return temp;
        }
        temp = verses66(number);
        if(temp != '')
        {
            return temp;
        }
    }
    catch(err){}
}

